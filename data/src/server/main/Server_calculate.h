#pragma once
#include "Server_func.h"
#include "Collision.h"
#include <cmath>
#include <stdlib.h>

class Restitution
{
public:
  Restitution();

  static int Player_restitution(CONTAINER Posdata);

private:
};